/** @type {import('./$types').LayoutLoad} */
export const load = ({ url }) => {
  return { url }
}

// /** @type {import('./$types').LayoutLoad} */
// export const load = async ({ url }) => {
//   return { url }
// }